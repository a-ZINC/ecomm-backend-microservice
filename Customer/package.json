{
  "name": "customer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "set NODE_ENV=prod && nodemon src/index.ts",
    "dev": "set NODE_ENV=dev && nodemon src/index.ts",
    "prisma:generate": "npx prisma generate --schema=./src/prisma/schema.prisma",
    "prisma:migrate": "npx prisma migrate dev --schema=./src/prisma/schema.prisma",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemon": "^3.1.4",
    "pg": "^8.12.0",
    "ts-node-dev": "^2.0.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.6",
    "prisma": "^5.18.0"
  }
}
